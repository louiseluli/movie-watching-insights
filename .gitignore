
# ─────────────────────────────────────────────────────────────
# Project: Movie Watching Insights (MWI)
# Purpose: Keep secrets, caches, and bulky data out of Git.
# ─────────────────────────────────────────────────────────────

# OS cruft
.DS_Store
Thumbs.db

# Python bytecode and build artifacts
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg-info/
dist/
build/
.eggs/

# Virtual environments & tools
.venv/
venv/
.env
.env.local
*.env  # any accidental .env.* files

# Jupyter
.ipynb_checkpoints/

# Editors/IDEs
.vscode/
.idea/

# Logs & profiles
*.log
logs/
.prof
*.prof
*.trace

# Data (raw/large/cache) — keep examples & tiny manifests only
# Adjust if you intentionally want to version certain small samples.
data/raw/**
!data/raw/.gitkeep
!data/raw/README.md

data/cache/**
!data/cache/.gitkeep
!data/cache/README.md

# Processed datasets are re-creatable; keep out by default.
data/processed/**
!data/processed/.gitkeep
!data/processed/README.md

# External API response caches
data/raw/external/**
!data/raw/external/.gitkeep
!data/raw/external/README.md

# IMDb non-commercial TSV dumps (large & frequently refreshed)
data/raw/imdb/**
!data/raw/imdb/.gitkeep
!data/raw/imdb/README.md

# Local DBs / query artifacts
*.db
*.duckdb
*.sqlite
*.sqlite3

# Reports/figures: allow exported images, ignore heavy intermediates
reports/figures/**.tmp
reports/figures/**.bak

# Dash/Streamlit temporary stuff
dashboards/.streamlit/secrets.toml
dashboards/.streamlit/**

# Node toolchains (only if you later add any)
node_modules/

# GitHub actions artifacts (if used)
.github/**/tmp/
