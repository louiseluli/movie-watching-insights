# ─────────────────────────────────────────────────────────────
# Movie Watching Insights (MWI) — Example environment variables
# Copy this file to `.env` and fill in REAL values locally.
# Never commit your real `.env`.
# ─────────────────────────────────────────────────────────────

# === Core paths (use absolute paths if you prefer) ===
MWI_DATA_DIR=./data
MWI_IMDB_DUMP_DIR=./data/raw/imdb
MWI_CACHE_DIR=./data/cache
MWI_PROCESSED_DIR=./data/processed

# Your IMDb export (CSV from the IMDb website; name yours as you like)
MWI_IMDB_WATCHLIST_CSV=./data/raw/imdb_watchlist.csv
# Optional: your IMDb ratings/history CSVs (leave empty if not used yet)
MWI_IMDB_RATINGS_CSV=
MWI_IMDB_HISTORY_CSV=

# === App/UI preferences ===
# Choose which dashboard to run by default: dash | streamlit
MWI_DASHBOARD_DEFAULT=dash

# Logging level: DEBUG | INFO | WARNING | ERROR
MWI_LOG_LEVEL=INFO

# Max workers for parallel API calls (tune to your network)
MWI_MAX_WORKERS=8

# Cache controls (in hours) for external API responses
MWI_API_CACHE_TTL_HOURS=168

# === IMDb Non-Commercial Datasets ===
# Place the gzipped TSVs in MWI_IMDB_DUMP_DIR:
#  - title.basics.tsv.gz, title.ratings.tsv.gz, title.crew.tsv.gz,
#  - title.principals.tsv.gz, title.akas.tsv.gz, title.episode.tsv.gz, name.basics.tsv.gz
# Nothing to set here; paths are derived from MWI_IMDB_DUMP_DIR.

# === OMDb API ===
OMDB_API_KEY=

# === TMDb API ===
TMDB_API_KEY=
TMDB_API_READ_ACCESS_TOKEN=

# === DoesTheDogDie (DDD) API ===
DDD_API_KEY=
# DDD requires headers:
#   Accept: application/json
#   X-API-KEY: <DDD_API_KEY>

# === Optional: Seedbox indexing (do not store passwords in git) ===
SEEDBOX_HOST=
SEEDBOX_USER=
SEEDBOX_PASS=
# Comma-separated paths on the remote server we’ll scan for movie files
SEEDBOX_PATHS=torrents/rtorrent,torrents/deluge,torrents/qbittorrent

# === Networking (optional) ===
# HTTP(S) proxy if you need it, e.g., http://localhost:8080
HTTP_PROXY=
HTTPS_PROXY=

# === Performance knobs (advanced; safe defaults) ===
MWI_POLARS_PARALLEL=true
MWI_HTTP_TIMEOUT_SECONDS=20
MWI_HTTP_RETRIES=3
MWI_HTTP_BACKOFF_SECONDS=1.5

# === Output formats ===
# Parquet is fast/columnar; add csv if you want both
MWI_OUTPUT_FORMATS=parquet

# === Locale/Time ===
# Used for time-based charts (your local tz). Europe/Warsaw is the default in this project context.
MWI_TIMEZONE=Europe/Warsaw
